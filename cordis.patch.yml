# dsh-session-folders bundle patch: mount the host plugin under its package name,
# resolved through the profile's node_modules.
- insert:
    - id: dsh-session-folders
      name: dsh-session-folders